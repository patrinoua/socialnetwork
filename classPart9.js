keydown=13 is enter -> send the action
use socket to emit an event to the server

io.socket.emit to emit the event that sent it

return{

}

render(){
    return
        <div id = "chatbox" ref={elem=>this.chatbox = elem}>
        </div>
}
